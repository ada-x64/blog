This is the directory for blog post authoring.
It is compiled as a first pass, _separately_ from the main content.
This way it can automatically generate an index from the metadata.
Each post is compiled separately, so no bundle is necessary.
